# 0studio Frontend Environment Variables
# Copy this file to .env and fill in your values

# ===========================================
# Supabase (Required for authentication)
# ===========================================
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# ===========================================
# Backend API (Required for payments & cloud)
# ===========================================
# For local development:
VITE_BACKEND_URL=http://localhost:3000

# For production (after deploying backend):
# VITE_BACKEND_URL=https://your-backend.us-east-1.awsapprunner.com

# ===========================================
# Stripe (Required for payments)
# ===========================================
# For development (test mode):
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...

# For production (live mode):
# VITE_STRIPE_PUBLISHABLE_KEY=pk_live_...

# Stripe price IDs for the Upgrade flow. Each must start with `price_`.
# Find these in Stripe Dashboard → Products → Pricing → API ID column.
VITE_STRIPE_PRO_PRICE_ID=price_...
VITE_STRIPE_ENTERPRISE_PRICE_ID=price_...

# ===========================================
# Notes
# ===========================================
# - See SUPABASE_SETUP.md for Supabase configuration
# - See STRIPE_SETUP.md for Stripe configuration
# - See AWS_BACKEND_DEPLOYMENT.md for deploying the backend
# - Never commit your actual .env file with real keys!
