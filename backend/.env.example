# 0studio Backend Environment Variables
# Copy this file to .env and fill in your values

# ===========================================
# AWS Configuration (for S3 file storage)
# ===========================================
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_REGION=us-east-1
S3_BUCKET_NAME=0studio-files



# Project invite emails (Amazon SES). Uses same AWS credentials as S3.
# From address must be verified in SES (e.g. noreply@yourdomain.com).
# Leave unset to skip sending invite emails.
# INVITE_FROM_EMAIL=noreply@yourdomain.com

# ===========================================
# Supabase Configuration (for auth verification)
# ===========================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ===========================================
# Stripe Configuration (for payments)
# ===========================================
# For development (test mode):
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# For production (live mode):
# STRIPE_SECRET_KEY=sk_live_...
# STRIPE_WEBHOOK_SECRET=whsec_... (use production webhook secret)

# ===========================================
# Server Configuration
# ===========================================
PORT=3000

# For development:
FRONTEND_URL=http://localhost:5173

# For production (allow all origins for desktop app):
# FRONTEND_URL=*

# ===========================================
# Weekly Founder Digest (admin-only)
# ===========================================
# Shared secret used by the GitHub Actions cron workflow when calling
# /api/admin/digest/run and /api/admin/digest/backfill.
# Generate with: openssl rand -hex 32
DIGEST_SHARED_SECRET=

# Slack incoming-webhook URL for the founder channel.
# Create at https://api.slack.com/apps → Incoming Webhooks.
SLACK_WEBHOOK_URL=

# Optional: SES "From" address for the digest email. Defaults to
# INVITE_FROM_EMAIL when unset. Must be SES-verified.
# DIGEST_FROM_EMAIL=digest@yourdomain.com

# ===========================================
# Notes
# ===========================================
# - SUPABASE_SERVICE_ROLE_KEY: Get from Supabase Dashboard → Settings → API → service_role key
# - STRIPE_SECRET_KEY: Get from Stripe Dashboard → Developers → API keys
# - STRIPE_WEBHOOK_SECRET: 
#   - For local dev: Run `stripe listen --forward-to localhost:3000/api/stripe/webhook`
#   - For production: Create webhook in Stripe Dashboard, copy the signing secret
# - See AWS_BACKEND_DEPLOYMENT.md for deploying to production
